package org.hotilsframework.collect;

/**
 * 用于 {@code org.hotilsframework.collect} 内部的收集器工具
 * @author hireny
 * @className CollectCollectors
 * @create 2020-06-17 19:59
 */
final class CollectCollectors {

//    static <T, K, V>Collector<T, ?, ImmutableMap<K, V>> toImmutableMap(
//            Function<? super T, ? extends K> keyFunction,
//            Function<? super T, ? extends V> valueFunction) {
//        Assert.notNull(keyFunction);
//        Assert.notNull(valueFunction);
//        return Collector.of(ImmutableMap.Builder<K, V>)
//    }
}
